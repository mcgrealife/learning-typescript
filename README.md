# learning-typescript

- added from github UI, now will try pulling the change into my local project
- it worked! I pulled, even though I changed the project name in github from `typescript` to `learning-typescript`. 